apiVersion: v1
kind: Service
metadata:
  name: entregas-service-service
spec:
  selector:
    app: entregas-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30083
  type: NodePort